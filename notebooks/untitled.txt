508189, 449124, 460110, 450721, 450909, 508589, 508595, 528353, 528588, 525823, 515614, 551623, 517353, 453249, 552913

XGB Best parameters: {'max_depth': 10, 'n_estimators': 900, 'learning_rate': 0.09127351971526927}, Best ROC-AUC CV value: 0.9821923454199311

1) eda: обрабатываем основные признаки по принципу - считаем топ 20 корреляций, строим распределения, считаем пропуски и общую информацию по признаку; оставшиеся признаки V - разбиваем на группы по 20 признаков, считаем матрицу корреляций между ними, по матрице составляем пары для отбора и по количеству уникальных элементов отбираем признаки из пар (подгрупп). либо на каждой группе делаем pca
2) feature selection:
    - начинаем с uid - вычисляем TransactionDay = TransactionDT / (24 * 60 * 60), далее используя card1, addr1, d1n (transactionDay - d1) вычисляем uid
    - на основе uid группируем признаки и считаем их std, mean
    - создаем другие новые признаки D4n, D10n, D15n
    - пользуемся топ1 решением и пишем, что многие из признаков id_00 - id_34 - шумовые, и удаляем их. далее merge identity с transaction и заполняем пропуски средним
    - получаем итоговые train и test, считаем корреляции, проводим доп отбор признаков, пользуясь одним из способов: лассо, корреляции, lgbm
4) baseline: lasso, isolation forest, one class svm
5) ensemble: xgboost, lightgbm, catboost, blending
6) mlp 